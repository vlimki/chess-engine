- [ ] `movegen` dir, one file for each piece, then a file to combine all of those to generate every one of the attack tables (at compile time)
- [ ] Implement very basic things - move pieces, capture pieces, etc.
